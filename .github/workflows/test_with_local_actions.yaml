name: Test Generate Release Version (Local)

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Generate Release Version
        id: version
        uses: ./

      - name: Verify Output
        run: |
          echo "========================================="
          echo "✅ 版本生成成功"
          echo "========================================="
          echo "📦 当前版本: ${{ steps.version.outputs.current_version }}"
          echo "🔧 测试版本: ${{ steps.version.outputs.test_version }}"
          echo "📊 Alpha 编号: ${{ steps.version.outputs.alpha_number }}"
          echo "🔢 Build 编号: ${{ steps.version.outputs.build_number }}"
          echo "========================================="

          # 验证输出不为空
          [[ -n "${{ steps.version.outputs.current_version }}" ]] || exit 1
          [[ -n "${{ steps.version.outputs.test_version }}" ]] || exit 1
          [[ -n "${{ steps.version.outputs.alpha_number }}" ]] || exit 1
          [[ -n "${{ steps.version.outputs.build_number }}" ]] || exit 1

          echo "✨ 所有输出验证成功"
